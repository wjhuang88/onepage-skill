## Summary

- Fix GitHub repository URLs across all documentation files.
- Add `metadata` (author, version) to SKILL.md frontmatter to match skill specification.

## Notable Changes

- Corrected 13 references from `wjhuang88/onepage` to `wjhuang88/onepage-skill` in README.md, README.zh-CN.md, and INSTALL.md.
- Added `metadata.author` and `metadata.version` fields to SKILL.md YAML frontmatter.

## Upgrade Notes

- If you installed v0.1.0, update by downloading the new release archive and replacing the `onepage/` directory in your agent's skill folder.
- No breaking changes to skill behavior or template output.
